# Local development overrides (loaded only by `vite`, never by `vite build`).
# Makes the React dev server talk to the LOCAL backend through the Vite
# proxy (/api -> http://127.0.0.1:8000). Production builds keep the default
# Railway API URL, so this file contains no secrets.
VITE_API_URL=/api